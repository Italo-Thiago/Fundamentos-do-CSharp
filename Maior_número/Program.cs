int firstValue = 500;
int secondValue = 600;
int largerValue = Math.Max(firstValue, secondValue);

// A biblioteca Math vem por padrão com a System
// Max e um método que compara duas variáveis e retorna a maior
Console.WriteLine(largerValue);